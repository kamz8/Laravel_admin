/* 
 * Kamil Żmijowski
 * jQuery plugin to add bootstrap alerts 
 * 
 */


(function($){
    $.fn.bt.Alert = function(options) {
        options = $.extend({
            //...parametry
        }, options);
 
        return this.each(function() {
            var $t = $(this);
            
        });
    }
})(jQuery);